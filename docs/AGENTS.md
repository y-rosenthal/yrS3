Follow the documentation rules defined in the repository root `AGENTS.md`.

These files must:

* contain exactly one `#` heading at the top
* use `##` or deeper for all other headings
* never include numbered headings
* include the required guard comment

